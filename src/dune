(ocamllex i18n)
(executable (name i18n) (modules i18n))

(rule
  (targets trans.ml)
  (deps i18n.exe)
  (action (run %{deps}
  /home/jsagot/workspace/geneanet.git/geneweb/gw_new/gw/lang/lex_utf8.txt,/home/jsagot/workspace/geneanet.git/geneweb/gw_new/gw/lang/geneanet_utf8.txt
 %{targets})
  )
)

(library
  (name gwd_lib)
  (wrapped false)
  (libraries geneweb jingoo)
  (modules ezgw data trans)
)

(executable
  (name gwd)
  (public_name gwd)
  (libraries gwd_lib geneweb jingoo)
  (modules gwd handler interp)
)

(executable
  (name marshaler)
  (libraries uucp jingoo)
  (modules marshaler)
)

; Dummy library used to generate doc
(library
  (name types)
  (modules types)
  (modules_without_implementation types)
)
