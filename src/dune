(ocamllex lexicon_parser)

(library
  (name marshaler_lib)
  (public_name gwd.marshaler_lib)
  (wrapped true)
  (libraries jingoo)
  (modules marshaler_lib)
)

(library
  (name gwd_lib)
  (public_name gwd.lib)
  (wrapped true)
  (libraries marshaler_lib geneweb jingoo)
  (modules ezgw data trans lexicon_parser handler interp)
)

(executable
  (name gwd)
  (public_name gwd)
  (libraries str geneweb.gwdb1 gwd_lib geneweb jingoo)
  (modules gwd)
)

(executable
  (name marshaler)
  (public_name marshaler)
  (libraries str uucp jingoo gwd_lib marshaler_lib)
  (modules marshaler)
)

; Dummy library used to generate doc
(library
  (name types)
  (modules types)
  (modules_without_implementation types)
)
